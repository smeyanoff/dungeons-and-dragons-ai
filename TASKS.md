# Задачи команды разработки (Sprint Backlog)

**Последнее обновление:** 2026-01-21  
**Текущий спринт:** Январь 2026 — Стабильность инфраструктуры + проверки/кубики (UX)

**Правило приоритизации:** баги/стабильность → игровые механики → UX.  
**Монетизация:** НЕ берём в работу в этом спринте.

---

## 🎯 Цель спринта

- **Сделать dev/docker и интеграционные тесты предсказуемыми** (без crash-loop и flaky).
- **Довести “проверки характеристик” до управляемого системного механизма**: DM запрашивает проверку инструментом, игрок бросает **в 1 тап**, результат надежно сохраняется в историю и RAG.

---

## ✅ Что уже подтверждено по коду (не таски на спринт)

- **Карта мира**: `/map`, inline-навигация `map_to_*`, хранение `GameSession.CurrentLocationID` и `GameSession.MapImagePath`.
- **События локаций (база)**: `LocationEventGenerator` + типы `WorldEventTypeLocation*` + `WorldEventRepository.GetByLocationID`.
- **DM tools**: есть `request_ability_check`; бросок игроком сейчас через команду `/roll`.
- **Генерация изображений**: есть `/image` и tool `GenerateImageTool`.

---

## 🔴 P0 — Блокеры (чинить в первую очередь)

### P0.1 ✅ Done — Docker: Qdrant помечается `unhealthy` в dev compose и ломает `depends_on: service_healthy`
**Источник:** `CODE_REVIEW.md` P0.1  
**Файлы:** `build/docker-compose.yml`, `build/docker-compose.prod.yml`

- **Задача:**
  - Исправить healthcheck Qdrant в `build/docker-compose.yml` (сейчас дергается `http://localhost:6333/health`, что для `qdrant/qdrant:v1.16.2` может быть невалидно).
  - В `build/docker-compose.prod.yml` заменить “нечестный” healthcheck (с `|| exit 0`) на проверку, которая реально фейлится при недоступности.
- **Критерии приемки:**
  - `docker compose -f build/docker-compose.yml up` → `dnd-qdrant` становится `healthy`.
  - `depends_on` больше не блокирует старт `bot` из‑за Qdrant.
  - Healthcheck в prod не маскирует падения.

### P0.2 ✅ Done — Docker: `bot` не должен уходить в crash-loop при пустом `TELEGRAM_BOT_TOKEN`
**Источник:** `CODE_REVIEW.md` P0.2  
**Файлы:** `build/docker-compose.yml`, `README.md`/`QUICKSTART.md`/`DEPLOY.md` (по необходимости)

- **Задача:**
  - Сделать запуск dev compose “без токена” безопасным: либо `profiles`, либо отключение сервиса, либо `restart: "no"` для dev, либо отдельная команда/таргет для запуска только infra.
  - Улучшить сообщение/доку: как запускать infra без бота.
- **Критерии приемки:**
  - При пустом токене `docker compose up` не создает бесконечный рестарт `dnd-bot`.
  - В логах/доках ясно написано, как включить бота.

### P0.3 ✅ Done — Интеграционные тесты: `duplicate key` по `game_sessions.chat_id` + общий flaky-state
**Источник:** `CODE_REVIEW.md` P0.3  
**Файлы:** `tests/integration/game_mechanics_test.go`, `tests/integration/*`

- **Задача:**
  - Изолировать тест-кейсы: **уникальный `chat_id` на тест** (например, на основе `t.Name()` + рандом/время) вместо одного константного `123456789`.
  - Привести cleanup к надежному варианту: `TRUNCATE ... CASCADE` или очистка всех таблиц, которые завязаны на сессию, одним способом.
- **Критерии приемки:**
  - `go test ./tests/integration -count=10` не падает из-за `idx_game_sessions_chat_id`.
  - Тесты можно запускать повторно на одной и той же БД (volume) без ручной очистки.

---

## 🟠 P1 — Высокий приоритет (стабильность/качество)

### P1.1 ✅ Done — GigaChat: `expires_at` вероятно в мс, код трактует как секунды
**Источник:** `CODE_REVIEW.md` P1.1  
**Файлы:** `pkg/gigachat/auth.go`

- **Задача:** нормализовать `expires_at` (ms→s) + sanity-check (год “58022” невалиден) + тест.
- **Критерии приемки:**
  - В логах токен истекает в разумные сроки (минуты/часы/дни), без “year 58022”.
  - Юнит‑тест покрывает оба варианта (секунды и миллисекунды).

### P1.2 ✅ Done — LLM: нестабильный JSON → флейки в критичных генерациях
**Источник:** `CODE_REVIEW.md` P1.2 + P1.3  
**Файлы:** `internal/game/application/campaign/*`, `internal/game/application/dm_analyzer/*`

- **Задача:** единый слой “строгий JSON + repair” для ответов LLM (запрет code fences, вырезание ```json, fallback-парсер), минимум для:
  - генерации кампании/мира;
  - DM analyzer.
- **Критерии приемки:**
  - В интеграционных прогонах снижается доля “not valid JSON / {} / незакрытые поля”.
  - Ошибки становятся диагностируемыми (лог содержит сырое + repaired, но без утечек чувствительных данных).

### P1.3 ✅ Done — Игровой UX: “технические” tool-сообщения не должны показываться игроку
**Источник:** `CODE_REVIEW.md` P1.4  
**Файлы:** `internal/telegram/bot.go` (+ слой формирования player-facing ответа)

- **Задача:** разделить internal/tool output vs player-facing narrative; добавить санитизацию/фильтр.
- **Критерии приемки:**
  - Игрок видит только художественный текст + понятный результат (“Ты выбросил 14 (d20=11 +3)”).
  - Отладочная инфа остается в логах/LLM monitoring, но не в чате.

---

## 🟡 P2 — Игровые механики (после P0/P1, без монетизации)

### P2.1 ✅ Done — Проверки характеристик: “в 1 тап” вместо ручного `/roll`
**Источник:** `PRODUCT_IDEAS.md` (Главный приоритет)  
**Файлы кандидаты:** `internal/game/application/dm_tools/character_tool.go`, `internal/telegram/bot.go`, `internal/game/domain/session/*`, `internal/game/domain/event/*`

- **Задача (MVP):**
  - При `request_ability_check` сохранять “ожидаемую проверку” в сессии (pending check).
  - В Telegram показывать inline‑кнопку **“🎲 Бросить d20”** (и при необходимости выбор advantage/disadvantage).
  - Привязать результат броска к pending check, сохранить в историю и проиндексировать в RAG.
- **Критерии приемки:**
  - Игрок проходит полный цикл проверки без ручного ввода команды.
  - Следующий бросок не “прилипает” к старой проверке.

### P2.2 ✅ Done — Guardrails против спама проверок (runtime, не только промпт)
**Источник:** `PRODUCT_IDEAS.md` P0.4  

- **Задача:** внедрить минимальные ограничители:
  - check budget (например, 1 значимая проверка на “сцену/ход”);
  - cooldown на повтор одинаковой проверки без эскалации ставок.
- **Критерии приемки:**
  - DM не может запросить повтор одной и той же проверки бесконечно подряд (система блокирует/предлагает описать без броска).

### P2.3 ✅ Done — События локаций: структурировать payload (options/suggested_checks/stakes)
**Источник:** `PRODUCT_IDEAS.md` P0.1 + P0.6  

- **Задача:** расширить модель событий локации (как минимум через структурированную `metadata`/payload), чтобы DM получал:
  - `hook`, `options (2-4)`, `suggested_checks (0-2)`, `stakes`, `status`.
- **Критерии приемки:**
  - Событие локации может быть показано игроку как “сцена + варианты”.
  - Данные события и исхода индексируются в RAG.

---

## 🟢 Backlog (не брать в этот спринт)

- **Мультиплеер/партия**, **гильдии/кланы**, **крафт**, **репутация с NPC** — крупные механики, вернемся после стабилизации P0/P1/P2.

---

## 📝 Процесс обновления

- Когда задача взята: пометить **⏳ In Progress**.
- Когда сделана: пометить **✅ Done** и обновить **`PRODUCT_IDEAS.md` / `CODE_REVIEW.md`** (что изменилось и какие риски сняты).
